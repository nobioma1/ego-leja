describe('[POST /api/users/reset-password] Reset user password', () => {
  it.todo('returns an error 400 if fields are not valid');

  it.todo('return a status of 200 if fields are valid');

  it.todo('does not store plain password');

  it.todo('reset user password and send response');
});
