import React from 'react';

import { DrawerLayout } from 'components/Drawer';

export const AddBorrowing = ({ disclosure }) => {
  return (
    <DrawerLayout disclosure={disclosure} title="Add Borrowing"></DrawerLayout>
  );
};
