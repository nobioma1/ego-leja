import React from 'react';

import { DrawerLayout } from 'components/Drawer';

export const AddCredit = ({ disclosure }) => {
  return (
    <DrawerLayout disclosure={disclosure} title="Add Credit"></DrawerLayout>
  );
};
